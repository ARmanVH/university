{
  "name": "test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tailwind": "npx tailwindcss -i ./style.css -o ./public/style.css --watch"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
